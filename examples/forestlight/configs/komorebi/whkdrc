# Forestlight — whkd keybindings (%USERPROFILE%\.config\whkdrc)
# Sintaxe própria do whkd (inspirada em skhd/sxhkd) — NÃO é YAML, apesar do nome comum
# "whkd.yaml" em alguns tutoriais desatualizados. Ver https://github.com/LGUG2Z/whkd.

.shell pwsh

# Foco entre janelas
alt + h : komorebic focus left
alt + j : komorebic focus down
alt + k : komorebic focus up
alt + l : komorebic focus right

# Mover janela
alt + shift + h : komorebic move left
alt + shift + j : komorebic move down
alt + shift + k : komorebic move up
alt + shift + l : komorebic move right

# Layout / estado
alt + t : komorebic toggle-tiling
alt + shift + f : komorebic toggle-float
alt + f : komorebic toggle-monocle
alt + p : komorebic toggle-pause

# Workspaces (I..IV, ver komorebi.json)
alt + 1 : komorebic focus-workspace 0
alt + 2 : komorebic focus-workspace 1
alt + 3 : komorebic focus-workspace 2
alt + 4 : komorebic focus-workspace 3

alt + shift + 1 : komorebic move-to-workspace 0
alt + shift + 2 : komorebic move-to-workspace 1
alt + shift + 3 : komorebic move-to-workspace 2
alt + shift + 4 : komorebic move-to-workspace 3

# Sair de um container empilhado
alt + shift + q : komorebic close
